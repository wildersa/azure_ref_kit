# Solutions

Complete composed examples live here.

A solution combines building blocks into a real customer-like scenario.

## Rule

Solutions should not hide new reusable logic. If something is reusable, extract it to `building-blocks/` or `shared/`.
